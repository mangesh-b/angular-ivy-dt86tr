import { Component, Input } from '@angular/core';

@Component({
  selector: 'dy-introduction',
  template: `
    <h2>Material Dynamic Form</h2>
    <p>The material-dynamic-form is an Angular 15 component that provides a HTML Form which loads JSON configuration.</p>
    <p>The component is responsible for displaying the HTML Form, validation, error messages.</p>
    <p>It supports Angular material fields and pre-build or custom themes.</p>
    <p><i>npm install material-dynamic-form</i></p>
  `,
  styles: [],
})
export class IntroductionComponent {}
